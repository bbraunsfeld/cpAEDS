name: constph
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pyyaml
  - matplotlib
  - seaborn
  - statsmodels
  - scipy
  - tqdm
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

